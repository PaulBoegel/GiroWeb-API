const chai = require('chai');
const expect = chai.expect;
const CardServiceController = require('../../src/controller/cardServiceController');
chai.use(require('chai-as-promised'));

describe('HelloTessService SaveTransaction', () => {
  let serviceFactory;
  it('should return an Error if dependencies are empty', async () => {});
});
